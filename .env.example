# HubSpot Private App token (server-side only, never sent to the client).
# Without it the app runs in Demo mode with generated sample data.
HUBSPOT_TOKEN=

# Optional access gate. If set, every page requires this password once per browser.
# Leave empty while previewing locally; set it before exposing the app on any URL.
DASHBOARD_PASSWORD=
